<!--
    descript 通用卡牌
    @Author: tangdm
    @Date: 20190904
-->
<template>
    <div class="dg-normal-card">
        <div class="dg-normal-card__info">
            <div style="float: right">
                <i slot="title-right" class="el-icon-refresh-right dg-color-text-45" >
                </i>
            </div>
            <h2 class="dg-normal-card__title">12,125</h2>
            <p class="dg-normal-card__subtitle dg-words-normal">本省行政案件总数</p>
            <div :class="$slots.content ? 'dg-normal-card__box' : ''">
                <slot name="content" v-if="$slots.content"></slot>
            </div>
        </div>
        <div :class="footerContent || $slots.footer ? 'dg-normal-card__foot' : ''">
            <slot name="footer" v-if="$slots.footer"></slot>
            <div v-else>{{ footerContent }}</div>
        </div>
    </div>
</template>

<script>
export default {
    name: "normalCard",
    props: {
        title: String,
        subtitle: String,
        footerContent: String
    },
    mounted() {
        // console.log(this);
    }
};
</script>
