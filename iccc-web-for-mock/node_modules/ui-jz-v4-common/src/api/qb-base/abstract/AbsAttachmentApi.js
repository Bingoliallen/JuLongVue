import { BaseApi } from "../BaseApi";
/**
 * 附件、图片上传和下载
 */
class AbsAttachmentApi extends BaseApi {
    constructor(module) {
        super(module);
    }
    /**
     * 保存上传文件
     * @param type  ZP-照片 FJ-附件
     * @param id    文件id
     */
    saveFiles(type, ids,loading,) {
        throw new Error("未实现...");
    }
    /**
     * 加载文件
     * @param type  ZP-照片 FJ-附件
     * @param id    文件id
     */
    loadFiles(type, ids,loading) {
        throw new Error("未实现...");
    }
    /**
     * 通过id获取文件路径
     * @param type  ZP-照片
     * @param cardid    身份证
     */
    getDatabyCardid(cardid,loading) {
        throw new Error("未实现...");
    }
}

export { AbsAttachmentApi };
