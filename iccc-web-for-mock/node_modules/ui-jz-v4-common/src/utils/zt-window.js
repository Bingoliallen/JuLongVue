/**
 * 数据请求处理类
 * @Author: huangjq
 * @Date:   2019-02-26
 * @Project jz
 */
export default {
    dlgH: function(h) {
        return Math.min(h, window.innerHeight - 150);
    },
    dlgW: function(w) {
        return Math.min(w, window.innerWidth - 100);
    }
};
