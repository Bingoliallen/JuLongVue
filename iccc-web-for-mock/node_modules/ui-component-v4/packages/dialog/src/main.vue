<template>
    <transition name="dialog-fade" @after-enter="afterEnter" @after-leave="afterLeave">
        <div class="el-dialog__wrapper" v-show="visible" @click.self="handleWrapperClick">
            <div
                role="dialog"
                aria-modal="true"
                :aria-label="title || 'dialog'"
                class="el-dialog dg-dialog"
                :class="[
                    { 'is-fullscreen': fullscreen, 'el-dialog--center': center /*'dg-dialog--vertical': vertical*/ },
                    customClass
                ]"
                ref="dialog"
                :style="style"
            >
                <div :class="['el-dialog__header', `dg-dialog__header`]">
                    <slot name="title">
                        <span class="el-dialog__title">{{ title }}</span>
                    </slot>
                    <button
                        type="button"
                        class="el-dialog__headerbtn"
                        aria-label="Close"
                        v-if="showClose"
                        @click="handleClose"
                    >
                        <i class="el-dialog__close el-icon el-icon-close"></i>
                    </button>
                </div>
                <div class="el-dialog__body dg-dialog__body" v-if="rendered">
                    <slot></slot>
                </div>
                <div class="el-dialog__footer dg-dialog__footer" v-if="$slots.footer">
                    <slot name="footer"></slot>
                </div>
            </div>
        </div>
    </transition>
</template>

<script>
import { Dialog } from 'element-ui';

export default {
    name: 'DgDialog',

    mixins: [Dialog],

    props: {
        vertical: {
            type: Boolean,
            default: false
        },

        headType: {
            type: String,
            default: ''
        }
    }
};
</script>
