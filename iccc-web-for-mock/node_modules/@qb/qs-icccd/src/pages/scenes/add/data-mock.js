/**
 * Created by hecg on 2020/10/22.
 */
import data from './data-save.cjs';

let { util, sceneId, getSceneModelList, getLeftList, getComponentOption, getPreview, getPublish } = data;

const _getSceneModelList = getSceneModelList;
getSceneModelList = async function(...arg) {
  return _getSceneModelList(...arg);
};

export { util, sceneId, getSceneModelList, getLeftList, getComponentOption, getPreview, getPublish };

export async function saveModelConfig(list) {
  console.log(
    'saveModelConfig',
    JSON.stringify(
      list.map(({ modelId, componentId, order, formBind, bind }) => {
        return {
          modelId,
          componentId,
          order,
          option: { formBind, bind }
        };
      })
    )
  );
  return true;
}

export async function publishScenes() {
  return true;
}
